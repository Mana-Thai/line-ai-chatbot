# mother-film-01 — 母と娘・1分の短編

タイの母娘の半生を9シーン・60秒で描く短編。**テキスト/キャプション/ロゴは一切入れない**
(ブリーフの指定)。ギフト動画パイプラインの `assemble.py` は文字入れ・紙テクスチャ転換を
行うため使わず、この注文専用の `build.py`(クロスフェード連結+ラスト暗転)で組み立てる。

## 構成

```
mother-film-01/
├── shotlist.json   # 正本: 9シーンの内容・Veoプロンプト・尺・仮編集のカメラワーク
├── build.py        # 生成 + 組み立て
├── stills/         # 各シーンの入力静止画 (sceneN.png = 生成用 / _original = 参考画像の原寸切り出し)
├── input/          # 各シーンの動画 (sceneN.mp4 = 本編 / previz_sceneN.mp4 = 仮編集)
└── output/         # 完成動画
```

尺の設計: シーン合計64.0秒 − クロスフェード0.5秒×8 = **60.0秒**。
配分は 7/7/7/6/6/7/8/9/7 秒で、感情の山(シーン8=母が娘の顔を包む)を最長の9秒にしている。

## 求める動きの定義(最重要)

本編は**画面内のすべてが動く実写映像**でなければならない。静止画をカメラで動かした
ものは不可。各シーンのプロンプトは以下の3層で書いてある(`motion_policy` 参照):

1. **主動作(1つだけ)** — 髪を編む手、皿に魚を移す、ポケットを二度叩く 等
2. **表情の変化** — まばたき、目を伏せる、唇を結ぶ、涙が溜まるが落ちない 等
3. **アンビエントな微動** — 髪・布・湯気・埃・草・光のゆらぎ・背景の人通り

主動作を2つ以上入れると顔や手が破綻するため、動きを増やすときは3の層だけ厚くする。
`negative_prompt` には `static frozen image` `cinemagraph` `ken burns zoom` 等を入れて、
一部だけが動く「動く写真」に落ちないようにしている。

## 仮編集(API不要・無料)

```bash
python build.py --previz     # → output/mother-film-01_previz.mp4
```

**これは本編ではない。** 参考画像にカメラワークを付けただけの、尺と並びだけを確認する
ための代用品で、上記の「画面内が動く」要件を満たしていない。参考画像も1コマ約320×180
しかないため画質も本編を表さない。構成合意用と割り切る。

## 本編(要 GEMINI_API_KEY・有料)

各シーンをVeoで生成してからつなぐ。9シーン分の生成費用がかかる。

```bash
export GEMINI_API_KEY=...
python build.py                  # 全シーン生成 → 組み立て
python build.py --scenes 3,7     # 特定シーンだけ作り直す
python build.py --assemble-only  # 生成済み素材をつなぐだけ
```

生成済みの `input/sceneN.mp4` は自動でスキップされるので、気に入らないシーンだけ
削除して再実行すれば作り直せる。生成の当たり外れがあるため、重要なシーン
(1・8・9)は複数回試して選ぶ想定。

### 本編の前に差し替えたいもの

参考画像は解像度が足りないため、**本番では各シーンの入力画像を720p以上で用意し直す**
必要がある。`stills/sceneN.png` を同名で置き換えれば `build.py` はそのまま動く。

### 人物の同一性

9シーンを個別に生成すると母と娘の顔が別人になるため、`refs/` に人物の参照画像を置く
(母は年齢が変わるので若年期/高年期の2枚、娘は幼少期/成人期の2枚。詳細は
[refs/README.md](refs/README.md))。`build.py` が `shotlist.json` の `characters` と
各シーンの `refs` を見て、そのシーンに出る人物の画像だけをVeoに渡す。
未配置の人物はスキップして続行するので、揃った人物から順に試せる。

## 未決事項

- **BGM**: ブリーフに指定がないため無音で出力している。付ける場合は別途選定が必要
- **アスペクト**: 16:9(1920×1080)で作成。LINE/リール用の縦型が必要なら要相談
